extends layout

block content
  h1 #{title}
  form(method='post', action='/bids/add')
    .form-group
      input(name='ride_id', type='hidden', value='#{ride_id}')
    .form-group
      label Passenger:
      input(name='passenger', type='text')
    .form-group
      label Amount:
      input(name='amount', type='text')
    input(type='submit', name='submit', value='Save')

    a(href='/rides/#{ride_id}') Back
